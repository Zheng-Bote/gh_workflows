name: "get next semver patch"

on:
  workflow_dispatch:
    inputs:
      Mayor:
        description: next SemVer is a Mayor
        type: boolean
        required: true
        default: false
      Minor:
        description: next SemVer is a Minor
        type: boolean
        required: true
        default: false
      Patch:
        description: next SemVer is a Patch
        type: boolean
        required: true
        default: true
  workflow_call:
    inputs:
      Mayor:
        description: next SemVer is a Mayor
        type: boolean
        required: true
        default: false
      Minor:
        description: next SemVer is a Minor
        type: boolean
        required: true
        default: false
      Patch:
        description: next SemVer is a Patch
        type: boolean
        required: true
        default: true

env:
  NEXT_VERSION_TYPE: Patch

jobs:
  bash-script:
    runs-on: ubuntu-latest

    steps:
      - name: check-out
        uses: actions/checkout@v4

      - name: Run my Mayor Action
        id: run-action-mayor
        if: ${{ inputs.Mayor == true }}
        uses: ./.github/actions/next_semver_mayor
        #uses: ./gh-a_page_list-tags@main
        with:
          Mayor: true
          Minor: false
          Patch: false
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run my Minor Action
        id: run-action-minor
        if: ${{ inputs.Minor == true }}
        uses: ./.github/actions/next_semver_minor
        #uses: ./gh-a_page_list-tags@main
        with:
          Mayor: false
          Minor: true
          Patch: false
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run my Patch Action
        id: run-action-patch
        if: ${{ inputs.Patch == true }}
        uses: ./.github/actions/next_semver_patch
        #uses: ./gh-a_page_list-tags@main
        with:
          Mayor: false
          Minor: false
          Patch: true
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Print Output
        id: output-mayor
        run: |
          echo "VSEMVER: ${{ env.VSEMVER }}" >> $GITHUB_STEP_SUMMARY
          echo "semver: ${{ env.semver }}" >> $GITHUB_STEP_SUMMARY
        env:
          VSEMVER: "${{ steps.run-action-mayor.outputs.vsemver }}"
          semver: "${{ steps.run-action-mayor.outputs.semver }}"

      - name: Print Output
        id: output-minor
        run: |
          echo "VSEMVER: ${{ env.VSEMVER }}" >> $GITHUB_STEP_SUMMARY
          echo "semver: ${{ env.semver }}" >> $GITHUB_STEP_SUMMARY
        env:
          VSEMVER: "${{ steps.run-action-minor.outputs.vsemver }}"
          semver: "${{ steps.run-action-minor.outputs.semver }}"

      - name: Print Output
        id: output-patch
        run: |
          echo "VSEMVER: ${{ env.VSEMVER }}" >> $GITHUB_STEP_SUMMARY
          echo "semver: ${{ env.semver }}" >> $GITHUB_STEP_SUMMARY
        env:
          VSEMVER: "${{ steps.run-action-patch.outputs.vsemver }}"
          semver: "${{ steps.run-action-patch.outputs.semver }}"
